# This is a Databricks asset bundle definition for pharma_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: pharma_dabs
  uuid: 7de2b6f1-869f-4218-9aa5-7dbec2ef641b

include:
  - dbx/*.yml
  - dbx/*/*.yml


sync:
  exclude:
    - ".github"          # GitHub Actions folder
    - "databricks.yml"   # bundle config itself
# Variable declarations. These variables are assigned in the dev/prod targets below.
# variables:
#   catalog:
#     description: The catalog to use
#   schema:
#     description: The schema to use
permissions:
  - user_name: sce21cs048@sairamtap.edu.in
    level: CAN_MANAGE
  - service_principal_name: 2118cc40-71a3-4e53-92ca-a60f61806747
    level: CAN_MANAGE
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      # host: https://adb-7405617499680447.7.azuredatabricks.net
    # variables:
    #   catalog: edwin_data_workspace
    #   schema: ${workspace.current_user.short_name}
    # presets:
    #   artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-7405608605218130.10.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/sce21cs116@sairamtap.edu.in to make sure we only have a single copy.
      root_path: /Workspace/Shared/Deployment
    # variables:
    #   catalog: edwin_data_workspace
    #   schema: prod
    permissions:
      - user_name: vsh.vegashp@gmail.com
        level: CAN_MANAGE
